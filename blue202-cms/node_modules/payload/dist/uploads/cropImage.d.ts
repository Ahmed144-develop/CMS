/// <reference types="node" />
import type { UploadedFile } from 'express-fileupload';
import sharp from 'sharp';
import type { UploadEdits } from './types';
export declare const percentToPixel: (value: any, dimension: any) => number;
type CropImageArgs = {
    cropData: UploadEdits['crop'];
    dimensions: {
        height: number;
        width: number;
    };
    file: UploadedFile;
    heightInPixels: number;
    widthInPixels: number;
};
export declare function cropImage({ cropData, dimensions, file, heightInPixels, widthInPixels, }: CropImageArgs): Promise<{
    data: Buffer;
    info: sharp.OutputInfo;
}>;
export {};
//# sourceMappingURL=cropImage.d.ts.map